package com.musicplayer.ui.viewmodel

/*
 * PlaylistViewModel was folded into SongsViewModel and
 * CategoryDetailViewModel, which now own the add-to-playlist flow together
 * with the track list they are showing. Kept as an empty file so stale
 * imports fail at compile time rather than resolving to unused state.
 */
