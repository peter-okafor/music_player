package com.musicplayer.ui.components

/*
 * TrackItem was replaced by TrackRow, which adds favourites, the animated
 * now-playing indicator and the reworked overflow menu. This file is kept
 * empty rather than deleted so existing imports fail loudly at compile time
 * instead of silently resolving to stale UI.
 */
